<?php

declare(strict_types=1);

it('toggles color mode between light and dark', function (): void {
    // Placeholder body — the real flow is exercised by the Vitest unit
    // suite (resources/js/Composables/useColorMode.test.ts). A browser
    // assertion will land here once the /admin route is reachable in CI
    // and the Pest browser cookie assertions are stabilised.
    expect(true)->toBeTrue();
})->skip('Playwright env-dependent — wire up in CI when Plan 2 admin route lands.');
